digits = [1, 2, 3, 4, 5, 6, 7, 100, 0]
print(min(digits)) # Returns lowest val from list
print(max(digits)) # Returns highest val from list
print(sum(digits))

names = ['bill', 'bob', 'karll']
print(f"\n{min(names)}") # Returns string w/ least letters
print(max(names)) # Returns string w/ most letters

# Cannot do sum on string-based lists
